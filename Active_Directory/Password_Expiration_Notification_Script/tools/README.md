# Tools

This folder is reserved for safe helper templates used during deployment.

For public repositories, do not commit files that include actual application credentials, generated credential files, logs, or environment-specific values.
